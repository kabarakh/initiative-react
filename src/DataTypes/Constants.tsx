export enum States {
  normal = '',
  readied = 'readied',
  delayed = 'delayed',
}

export enum ListTypes {
  prepare = 'prepare',
  combat = 'combat',
}

export enum ParticipantTypes {
  player = 'player',
  npc = 'npc',
}
